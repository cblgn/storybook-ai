# Optional: replace with a supported provider:model identifier.
# STORYBOOK_MODEL=provider:model-name
# STORYBOOK_GENERATION_TIMEOUT_SECONDS=180
# For an API-key provider, set its environment variable before starting uvicorn.
# OPENAI_API_KEY=replace-with-your-api-key
