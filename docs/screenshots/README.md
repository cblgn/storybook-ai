# Aperçus du bootstrap

Captures de l’interface locale sur ordinateur (1440 px) et mobile (390 px).
Les réponses HTTP sont simulées avec `backend/tests/fixtures/story.json` ;
ces images illustrent le rendu et ne constituent pas une validation d’un vrai LLM.
Les tests Playwright distincts valident la connexion à FastAPI avec un modèle
PydanticAI déterministe.
